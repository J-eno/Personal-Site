import Typography from "typography"
import doelgerTheme from "typography-theme-doelger"

const typography = new Typography(doelgerTheme)

export const { scale, options, rhythm } = typography
export default typography